# iPath · GitHub Pages público optimizado

Versión preparada para el Release público `assetsipath`, tag `catalogoipath`.

## Ajustes de esta edición

- Nombres de los adjuntos enlazados exactamente como aparecen en el Release plano.
- Conversión automática de nombres `ChatGPT Image ...` a `ChatGPT.Image...`.
- Conversión de iconos `(1)` a `.1`, `(10)` a `.10`, etc.
- Portadas del catálogo cargadas bajo demanda.
- Portada principal precargada con prioridad.
- Vídeo sin descarga inicial: solo se solicita cuando el usuario lo selecciona.
- Eliminado el recorrido automático del catálogo.
- Capacidad tecnológica representada sin descargar seis iconos de varios megabytes.
- Textura neutra de hormigón incrustada directamente en `index.html` para formatos y capas.
- La lámina `texturas-cueva-board.webp` se conserva exclusivamente en la sección de acabados.

## Publicación

Sustituye en el repositorio los archivos de este paquete y conserva el Release. No necesitas una carpeta `assets`.

Comprueba primero:

```text
https://TU-USUARIO.github.io/TU-REPOSITORIO/release-check.html
```
