(() => {
  'use strict';
  window.IPATH_RELEASE = Object.freeze({
    // Déjalos vacíos cuando el Release está en el mismo repositorio que GitHub Pages.
    // También puedes indicar un repositorio público distinto para los recursos.
    owner: '',
    repository: '',
    tag: 'catalogoipath',
    releaseName: 'assetsipath',
    version: '4.0.0'
  });
})();
